# CODEOWNERS for the ARM64 package-admission governance gate (PR-0).
#
# The rule registry and the debt ledger are the only two files that grant
# any tolerance in an otherwise fail-closed gate (see
# .ci/arm64-admission-guard.py). Any change to either MUST be reviewed and
# approved by a repository owner before it can be merged -- this is only
# effective in combination with the "Require review from Code Owners"
# branch protection setting on this repository's default branch (see the
# PR description for the exact setting verified/applied).
.ci/arm64-debt-ledger.tsv @crutkas
.ci/arm64-rules.toml @crutkas
